{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y openjdk-17-jdk && cd backend/rockxy/rockxy && ./mvnw clean install -DskipTests"
  },
  "deploy": {
    "startCommand": "apt-get update && apt-get install -y openjdk-17-jre-headless -y && java -Dserver.port=$PORT -jar backend/rockxy/rockxy/target/*.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}